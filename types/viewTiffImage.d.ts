import type { ISizeCalculationResult } from './types/interface';
export default function viewTiffImage(isBigEndian: boolean, adjust: (position: number, minimumSize: number) => DataView): ISizeCalculationResult;
